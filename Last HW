# Вариант 5
# (через функцию) Дано действительное положительное число a и целоe число n. Вычислите an. 
# Решение оформите в виде функции power(a, n). (Стандартной функцией возведения в степень пользоваться нельзя.)

def power(a, n):
    return a**n
def power1(a, n):
    for _ in range (n):
        a*=a
    return a
